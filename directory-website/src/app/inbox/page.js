import React from 'react';
import { Typography } from '@mui/material';

const InboxPage = () => {
  return (
    <div>
      <Typography variant="h4">Inbox</Typography>
      <Typography>This is the inbox page.</Typography>
    </div>
  );
};

export default InboxPage;
