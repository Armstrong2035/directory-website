import React from 'react';
import { Typography } from '@mui/material';

const LogoutPage = () => {
  return (
    <div>
      <Typography variant="h4">Logout</Typography>
      <Typography>This is the logout page.</Typography>
    </div>
  );
};

export default LogoutPage;
